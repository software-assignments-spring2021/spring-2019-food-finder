{
  "name": "Food-Finder",
  "version": "1.0.0",
  "description": "An app to help people decide where to eat",
  "main": "test.js",
  "homepage": "https://nyu-software-engineering.github.io/food-finder/",
  "scripts": {
    "test": "react-scripts test",
    "testit": "npm install nyc && npm install --save-dev mocha && npm install --save-dev chai && cd Test && nyc mocha",
    "installit": "npm install && cd test_2 && npx create-react-app the-app",
    "setuppc": "cd test_2 && cd the-app && rmdir /Q /S src && mkdir src",
    "setupunix": "cd test_2 && cd the-app && rm -rf src",
    "startitpc": "cd test_2 && xcopy my-app\\src the-app\\src /e",
    "startitunix": "cd test_2 && cp -R my-app/src the-app",
    "runit": "cd test_2 && cd the-app && npm start",
    "start": "react-scripts start",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "eject": "react-scripts eject"
  },
  "contributors": [
    "Andrew",
    "Ashwin",
    "Max",
    "Tika",
    "Vinnie"
  ],
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.0.2"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "dependencies": {
    "@material-ui/core": "^3.9.2",
    "@material-ui/icons": "^3.0.2",
    "axios": "^0.18.0",
    "gh-pages": "^2.0.1",
    "mongoose": "^5.5.0",
    "nodemon": "^1.18.11",
    "nyc": "^13.3.0",
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-scripts": "^2.1.8",
    "typeface-roboto": "0.0.54"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],

  "proxy": "http://localhost:5000"
}